schema_version: "1.1"
name: 01-write-es-client-email
runs: 3
plugins: [../../skills/write-like-me]
context:
  scaffold_script: scaffold.sh
execution:
  max_turns: 20
  timeout_seconds: 180
  allowed_tools: [Read, Glob, Grep, Skill]
  prompt: |
    escríbeme el correo para avisar al cliente de que el despliegue se retrasa hasta el jueves
