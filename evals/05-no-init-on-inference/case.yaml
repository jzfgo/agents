schema_version: "1.1"
name: 05-no-init-on-inference
runs: 3
plugins: [../../skills/write-like-me]
context:
  scaffold_script: scaffold.sh
execution:
  max_turns: 8
  timeout_seconds: 120
  allowed_tools: [Read, Glob, Grep, Skill]
  prompt: |
    that doesn't sound like me at all, just start over from my blog
